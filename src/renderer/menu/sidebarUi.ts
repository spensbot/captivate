/** Sidebar nav strip width (0.8rem padding × 2 + 1.7rem icon). */
export const SIDEBAR_STRIP_WIDTH = '3.3rem'

/** Drag hit area for the master wide fader (~full inner card width). */
export const SIDEBAR_MASTER_FADER_RADIUS_REM = 1.36
